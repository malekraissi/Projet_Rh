<form method="post" action="{{ path('salarie_etrange_delete', {'id': salarie_etrange.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salarie_etrange.id) }}">
    <button class="btn">Delete</button>
</form>
