<form method="post" action="{{ path('type_conge_delete', {'id': type_conge.id}) }}" onsubmit="return confirm('Vous etes sur?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_conge.id) }}">
    <button class="btn-primary">Supprimer</button>
</form>
