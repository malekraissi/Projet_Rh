{% extends 'base.html.twig' %}

{% block title %}Pole{% endblock %}

{% block body %}
    <h1>Pole</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Nom</th>
                <td>{{ pole.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pole_index') }}">liste de poles</a>

    <a href="{{ path('pole_edit', {'id': pole.id}) }}">Modifier</a>

    {{ include('pole/_delete_form.html.twig') }}
{% endblock %}
