<form method="post" action="{{ path('pole_delete', {'id': pole.id}) }}" onsubmit="return confirm('Vous etes sur?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pole.id) }}">
    <button class="btn-primary">Supprimer</button>
</form>
