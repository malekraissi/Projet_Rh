{% extends 'base.html.twig' %}

{% block title %}Pole {% endblock %}

{% block body %}
    <h1>Poles</h1>

    <table class="table">
        <thead>
            <tr>

                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for pole in poles %}
            <tr>

                <td>{{ pole.nom }}</td>
                <td>
                    <a href="{{ path('pole_show', {'id': pole.id}) }}">Afficher</a>
                    <a href="{{ path('pole_edit', {'id': pole.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">pas de data</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('pole_new') }}">Créer pole</a>
{% endblock %}
