{% extends 'base.html.twig' %}

{% block title %}Equipe{% endblock %}

{% block body %}
    <h1>Equipe</h1>

    <table class="table">
        <tbody>
            <tr>

                <td>{{ equipe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ equipe.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('equipe_index') }}"> liste des equipes</a>

    <a href="{{ path('equipe_edit', {'id': equipe.id}) }}">Modifier</a>

    {{ include('equipe/_delete_form.html.twig') }}
{% endblock %}
